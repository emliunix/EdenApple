/**
 * LISP Expr:
 *
 * const val
 * ref symbol
 * let expr
 * define expr
 * if expr
 * lambda [symbol] expr
 * call expr
 */

(function(g) {

    function compile(lst) {
	
    }
    
    g.compile = null;
})(this)
